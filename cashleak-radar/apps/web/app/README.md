# Web app

Routes currently included:

- `/` landing + product story
- `/dashboard` cash-risk dashboard
- `/import` import workflow
- `/anomalies` cash leak explorer
- `/customers` customer risk
- `/evidence` evidence timeline
- `/recovery` recovery center
- `/recovery/drafts` human approval view
- `/demo` live API demo
- `/settings` workspace settings
