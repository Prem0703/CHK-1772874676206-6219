# Import workflow

The first MVP supports safe synthetic/demo CSV workflows. Real file uploads should be wired to object storage, malware scanning, MIME validation, evidence hashing and asynchronous processing before production use.
