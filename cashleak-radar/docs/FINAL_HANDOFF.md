# Handoff

CashLeak Radar is isolated under `cashleak-radar/` on the `cashleak-radar` branch. The original repository application is intentionally untouched.

The current implementation is a demo-ready foundation. Before production, wire PostgreSQL persistence, object storage, real authentication, OCR, a structured-output LLM provider, background workers, and real integrations.
